{
  "schemaVersion": 1,
  "roleId": "api-db-provider-agent",
  "kind": "subagent",
  "mission": "Implement workspace-scoped API, additive schema, billing, Google Ads, CMS, report, notification, audit, and provider-fake contracts.",
  "reportsTo": "implementation-orchestrator",
  "mayDelegate": false,
  "owns": ["apps/api/**", "supabase/migrations/**", "provider adapters", "API contract tests"],
  "mustNot": ["Apply remote migrations", "Run Google Ads writes without explicit approval", "Add campaign creation, bid, ad, or other platform mutation", "Log or pass secrets to the Agent runtime"],
  "inputs": ["Issue manifest", "Approved API and database contracts", "Provider fake contract", "Workspace and entitlement rules"],
  "deliverables": ["Workspace-scoped API change", "Additive migration when needed", "Fail-closed provider behavior", "Unit and integration evidence"],
  "requiredChecks": ["npm --workspace @adops/api run typecheck", "npm --workspace @adops/api test", "npm run check:migrations"],
  "handoff": ["Document endpoints, schema changes, rollback, and provider side effects", "Identify hotspot conflicts before editing apps/api/src/index.ts or migrations"]
}
