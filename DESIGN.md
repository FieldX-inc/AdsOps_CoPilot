# AdOps Advisor UI/UX正本

Updated: 2026-07-21

## 1. プロダクトの感触

AdOps AdvisorはLPではなく、広告担当者が毎日開く業務画面である。緑を主色にした穏やかなテーマ、左固定サイドバー、白いカード、明確な状態表示を基本とする。AIが勝手に変更する感じではなく、数値と理由を見て人が責任を持って承認できる安心感を優先する。

## 2. アプリケーション構成

メインナビは次の順とする。

1. ダッシュボード
2. 広告準備
3. ヘルプ
4. データ連携

BI分析とAdコラムの独立ページは設けない。AIチャットは独立ページにせず、右下のランチャーまたは画面内CTAから幅400px前後の右ドロワーとして開く。Human Tasksも独立ページにせず、AI回答内の手順、ToDo、ダッシュボードの次アクションとして扱う。

## 3. 画面要件

### ダッシュボード

- 期間、媒体、広告アカウントの3フィルタ
- 一言コメント、KPI、トレンド、異常度、キャンペーン表、AI提案、異常一覧
- 最新のAI write candidateが実データに存在する場合は確認モーダルへのCTA
- loading、空データ、同期失敗、部分データ、モック利用中を明示

### 広告準備

- 過去セッションを再開できる、一問一答の質問票
- チャット履歴、吹き出し、composer、Agentの会話演出は使わない
- 初期質問票はラジオボタン中心とし、必要な補足だけ各質問の任意入力にする
- 進捗、現在の質問、戻る、保存して次へ、回答一覧を一本の導線に置く
- 主画面はカードを積み重ねず、余白、罫線、タイポグラフィで区切る
- 未回答の場合は次の必須質問を一つに絞る
- 完了後はキャンペーン作成案と、媒体管理画面で人が確認・作成する手順を生成
- キャンペーンをAPIで作成するCTAや実行済み表示は置かない

### ヘルプ

- キーワード検索、カテゴリ一覧、記事詳細の3要素を最小構成とする
- 操作手順、KPIの見方、OAuth連携、承認付きwriteの注意点を掲載する
- 正常時はmicroCMSの公開コンテンツを表示し、未設定または取得失敗時は最小ヘルプを表示する
- フォールバック中は「基本ヘルプを表示中」と明示し、空画面にしない

### データ連携

- OAuth接続状態、MCC配下のclient探索、同期、最終同期、エラー、監査履歴を中心とする
- MCC自体の同期を拒否し、client accountを明示選択する
- status/budget writeのbefore/after、audit ID、復元関係を確認できる

### 設定・課金

- 現在プラン、請求周期、人数/アカウント上限、AI利用率、リセット日
- Stripe Portalへの導線
- 3日レポート一覧と通知ON/OFF
- owner/adminのメンバー招待
- Premiumの予約ボタン。アプリ内予約管理は作らず、URL未設定時は`https://example.com`へ遷移する

## 4. ロックとアップグレード

機能が利用できない場合も非表示にしない。ロック状態、現在のプランで継続する機能、解除される機能、プラン変更導線を同じ面に出す。

- ミニマムのAIドロワーはロックするが、3日レポートが継続することを示す
- ミニマムのwrite CTAは対象と価値を見せた上でロックする
- AI上限後も履歴、レポート、設定への導線を残す

## 5. 高リスクwriteモーダル

確認モーダルには次をすべて表示する。

- 対象account/campaign
- Google Adsから実時間で取り直した現在値と観測時刻
- 変更後の値
- 根拠、リスク、ロールバック条件、観察期間
- 明示確認チェック
- 実行後のbefore → after、audit ID、元に戻す導線

競合時はそのまま実行せず、再previewを求める。

## 6. 表示とアクセシビリティ

- KPIの算出不能値は`-`
- 色だけでなくテキスト/アイコンで状態を示す
- modalは`role=dialog`、`aria-modal=true`、明示的なtitle/closeを持つ
- フォームはlabel、エラー、実行中disabledを持つ
- 顧客向けにtoken数、推定原価、Price ID、内部IDを表示しない

## 7. production制約

`VITE_APP_ENV=production`では`?preview=setup`、認証/billing bypass、モックダッシュボード切替を強制無効化する。本番データがない場合にモックを実データとして見せない。
